# Mise configuration for LangGraph project
# https://mise.jdx.dev/
#
# Tasks have been extracted to .mise/tasks/ for scalability.
# Each file in .mise/tasks/ is a standalone task script.
# Run 'mise tasks' to see all available tasks.

[tools]
python = "3.14"

[env]
_.python.venv = ".venv"
